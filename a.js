let numbers = [10, 20, 30, 40, 50];  // Example array with 5 numerical values
let sum = 0;

for (let i = 0; i < numbers.length; i++) {
    sum += numbers[i];
}

console.log("The sum of array values is: " + sum);
